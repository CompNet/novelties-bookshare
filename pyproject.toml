[project]
name = "novelties-bookshare"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "nltk>=3.9.1",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
